# 众爱联盟 - 志愿者积分管理平台演示文档

## 一、项目概述

众爱联盟是一个志愿者积分管理平台，旨在通过积分激励机制鼓励更多人参与公益活动和捐赠行为。

### 项目架构
- **后端服务**: Nuxt 4 + PostgreSQL
- **移动端**: UniApp 微信小程序
- **管理后台**: Nuxt + Element Plus

### 核心功能模块
1. **活动管理** - 创建、发布、管理公益活动
2. **积分系统** - 积分规则配置、积分发放、积分消费
3. **捐赠管理** - 资金/物资捐赠管理与审核
4. **用户管理** - 志愿者注册、实名认证、用户冻结

---

## 二、启动指南

### 2.1 环境要求
- Node.js >= 20.x
- PostgreSQL >= 16.x

### 2.2 启动后端服务

```bash
# 进入服务器目录
cd /workspace/server

# 安装依赖
npm install

# 设置环境变量并启动
export $(cat .env | xargs) && npm run start
```

### 2.3 服务器配置文件 (.env)

```env
JWT_SECRET=zhongai-alliance-jwt-secret-key-2024
WECHAT_APP_ID=wx1234567890123456
WECHAT_APP_SECRET=wechat-app-secret-1234567890
ID_CARD_ENCRYPTION_KEY=1234567890abcdef
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/zhongai_alliance
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123456
```

### 2.4 创建演示数据

```bash
# 初始化管理员账户
npm run seed:admin

# 创建演示数据（用户、活动、捐赠、积分规则）
export $(cat .env | xargs) && node scripts/seed-demo.cjs
```

---

## 三、管理后台使用

### 3.1 登录

- **访问地址**: http://localhost:3000/admin/login
- **用户名**: admin
- **密码**: admin123456

### 3.2 主要功能模块

| 模块 | 功能说明 |
|------|----------|
| 活动管理 | 查看、创建、编辑、发布公益活动 |
| 用户管理 | 查看用户列表、冻结/解冻用户、查看详情 |
| 捐赠管理 | 审核捐赠申请、查看捐赠记录 |
| 积分管理 | 查看积分规则、调整用户积分 |

---

## 四、API接口

### 4.1 认证接口

```bash
# 管理员登录
curl -X POST http://localhost:3000/api/auth/admin-login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123456"}'
```

### 4.2 活动接口

```bash
# 获取活动列表（需认证）
curl -H "Authorization: Bearer <token>" http://localhost:3000/api/activities

# 创建活动（需认证）
curl -X POST http://localhost:3000/api/activities \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "新活动",
    "category": "environment",
    "description": "活动描述",
    "location": "活动地点",
    "startTime": "2026-06-01 09:00:00",
    "endTime": "2026-06-01 12:00:00",
    "checkinRadius": 500,
    "maxParticipants": 30,
    "rewardPoints": 100
  }'
```

### 4.3 用户接口

```bash
# 获取用户列表（管理员权限）
curl -H "Authorization: Bearer <token>" http://localhost:3000/api/admin/users

# 获取荣誉等级列表
curl http://localhost:3000/api/users/honor-levels
```

### 4.4 捐赠接口

```bash
# 获取捐赠列表（需认证）
curl -H "Authorization: Bearer <token>" http://localhost:3000/api/donations

# 审批捐赠（管理员权限）
curl -X PATCH http://localhost:3000/api/donations/<id>/approve \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"pointsToGrant": 500, "reviewRemark": "审批通过"}'
```

### 4.5 积分接口

```bash
# 获取积分余额（需认证）
curl -H "Authorization: Bearer <token>" http://localhost:3000/api/points/balance

# 获取积分规则
curl -H "Authorization: Bearer <token>" http://localhost:3000/api/points/rules

# 调整积分（管理员权限）
curl -X POST http://localhost:3000/api/points/adjust \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"userId": "<user-id>", "points": 100, "reason": "奖励积分"}'
```

---

## 五、演示数据

### 5.1 预置用户

| 角色 | 用户名/手机号 | 说明 |
|------|--------------|------|
| 管理员 | admin/admin123456 | 系统管理员账户 |
| 志愿者 | 13800138001 | 志愿者张三 |
| 志愿者 | 13800138002 | 志愿者李四 |
| 志愿者 | 13800138003 | 志愿者王五 |

### 5.2 预置活动

| 活动名称 | 分类 | 时间 | 积分奖励 | 状态 |
|----------|------|------|----------|------|
| 社区环保清洁活动 | 环保 | 2026-06-01 | 100 | 已发布 |
| 关爱孤寡老人活动 | 敬老 | 2026-06-08 | 80 | 已发布 |
| 贫困儿童助学活动 | 教育 | 2026-06-15 | 120 | 草稿 |

### 5.3 预置捐赠

| 类型 | 金额/价值 | 状态 | 捐赠人 |
|------|-----------|------|--------|
| 资金捐赠 | 500元 | 已审批 | 志愿者张三 |
| 物资捐赠 | 300元 | 待审核 | 志愿者李四 |

---

## 六、技术架构

### 6.1 后端技术栈

```
┌─────────────────────────────────────────────────────────┐
│                    Nuxt 4 Application                   │
├─────────────────────────────────────────────────────────┤
│  Pages (Vue 3)          API Routes            SSR      │
│  - admin/                - /api/auth/        - admin/* │
│  - login                 - /api/activities/            │
│                          - /api/users/                 │
│                          - /api/donations/             │
│                          - /api/points/                │
├─────────────────────────────────────────────────────────┤
│                    Nitro Server                        │
├─────────────────────────────────────────────────────────┤
│              Services Layer                            │
│  - activity.service.ts    - checkin.service.ts         │
│  - auth.service.ts        - donation.service.ts        │
│  - user.service.ts        - points.service.ts          │
├─────────────────────────────────────────────────────────┤
│              Database Layer (PostgreSQL)               │
│  - drizzle-orm            - migrations                 │
│  - schema definitions     - seed scripts               │
└─────────────────────────────────────────────────────────┘
```

### 6.2 数据库表结构

| 表名 | 说明 |
|------|------|
| users | 用户信息表 |
| activities | 活动信息表 |
| activity_registrations | 活动报名表 |
| activity_checkins | 活动签到表 |
| donations | 捐赠信息表 |
| point_accounts | 积分账户表 |
| point_transactions | 积分交易记录表 |
| point_rules | 积分规则表 |

---

## 七、移动端小程序

### 7.1 项目结构

```
miniapp/
├── src/
│   ├── pages/           # 页面目录
│   │   ├── index/       # 首页
│   │   ├── activity/    # 活动列表/详情
│   │   ├── donation/    # 捐赠管理
│   │   ├── points/      # 积分中心
│   │   ├── user/        # 用户中心
│   │   └── login/       # 登录页面
│   ├── utils/           # 工具函数
│   │   ├── request.ts   # 请求封装
│   │   └── auth.ts      # 认证管理
│   └── stores/          # 状态管理
│       └── user.ts      # 用户状态
└── package.json
```

### 7.2 构建命令

```bash
cd /workspace/miniapp
npm install --legacy-peer-deps
npm run build:mp-weixin
```

---

## 八、安全说明

1. **JWT认证**: 使用HS256算法进行token签名
2. **密码加密**: 使用bcryptjs进行密码哈希存储
3. **身份证加密**: 使用AES-256-GCM算法加密身份证信息
4. **权限控制**: 区分管理员和志愿者角色权限
5. **CORS配置**: 配置跨域访问白名单

---

## 九、常见问题

### Q: 数据库连接失败？
A: 确保PostgreSQL服务已启动，检查DATABASE_URL配置

### Q: JWT密钥错误？
A: 确保JWT_SECRET环境变量已正确设置

### Q: API返回401未授权？
A: 需要在请求头中携带Authorization: Bearer <token>

### Q: 小程序构建失败？
A: 确保已安装sass依赖: `npm install sass -D --legacy-peer-deps`

---

## 十、联系信息

- 项目文档: /workspace/docs/
- 接口文档: 各API端点支持OPTIONS请求获取详细信息
